import React from "react";
import * as B from "./styles";

const Button = ({ children, redirect, disabled, variant = 'primary', type = "button", onClick, full}) => {
    
    return (
       <B.Button></B.Button>
    );
}
export default Button;